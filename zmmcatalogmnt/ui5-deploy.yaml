# yaml-language-server: $schema=https://sap.github.io/ui5-tooling/schema/ui5.yaml.json

specVersion: "3.1"
metadata:
  name: customer.porky.zmmcatalogmnt
type: application
builder:
  resources:
    excludes:
      - /test/**
      - /localService/**
  customTasks:
    - name: deploy-to-abap
      afterTask: generateCachebusterInfo
      configuration:
        target:
          destination: DEV
          url: https://dev.porky.com
          client: '100'
        app:
          name: ZMMCATALOGMN
          package: zfiori1
          transport: DEVK915314
        exclude:
          - /test/
