sap.ui.define([], function () {
	"use strict";

	// Copy this file to AIConfig.js (gitignored - never commit your real key) and fill in a real
	// OpenRouter key from https://openrouter.ai/keys. Keep it on a restricted/low-limit key, since
	// it ships to the browser - see the warning in AIHelper.js.
	return {
		OPENROUTER_API_KEY: "sk-or-v1-REPLACE-ME"
	};
});
